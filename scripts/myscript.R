#val's workshop for git
#Nov 26, 2024

library(tidyverse)

dogs <- 1
cats <- 1000
hamsters <- -5000

#always name the remote as origin
#then branch is main
#then checkout if get message main already exists
